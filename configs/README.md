# Configuration location

The command runner takes explicit options; `openmua2.json` is descriptive metadata only. Keep runtime-generated profile and controller settings under `.local/user/Config`; the local launcher never overwrites them. No hardware-tested MUA2 controller profile is supplied. Store reviewed, nonprivate reusable configuration examples here when they have been validated.
