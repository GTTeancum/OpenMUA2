# Fresh LOCAL01 evidence

`VALIDATION.json` describes checks performed while preparing this local repo.
The source audit compares every delivered upstream byte to its pinned input or documented production patch.
The generated C comparison uses the freshly built recompiler and the retained original DOL.
`native-audit.json` checks the retained MG01 module; it is not a new module or gameplay test.
Workspace tests use real temporary Git repositories and backup restoration, but mock disc extraction publication.
Windows wrappers and the full game build/run have not been executed. Historical matrices/runs are separate under docs/history and recovery.
