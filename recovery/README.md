# Retained recovery material

`MG01/` contains actual surviving build patches and helper sources. `FPC01/` contains the complete separate conversion-helper repair, its guarded patch tools, tests and historical evidence. These are preserved for audit and reconstruction, not invoked automatically over your new edits.

**Read `../docs/CURRENT-STATUS.md` first.** Any old request inside historical FPC01 notes to upload the missing MG02 ZIP is obsolete. That archive was not delivered successfully and must not be requested from the user. No MG02 or MR01 source/module is hidden in this directory.

The five applicable production file edits are already present under `../project`. Do not blindly apply historical whole-file replacements on top of later local changes. The current cumulative diff is `../patches/LOCAL01-recovered-production.patch`.
